(()=>{
const SIZE=4,TARGET=2048,$=s=>document.querySelector(s);let board=[],score=0,best=0,won=false,keepPlaying=false,startedAt=0;
const empty=()=>Array.from({length:SIZE},()=>Array(SIZE).fill(0));
const clone=b=>b.map(r=>r.slice());
const addTile=()=>{const cells=[];for(let r=0;r<SIZE;r++)for(let c=0;c<SIZE;c++)if(!board[r][c])cells.push([r,c]);if(cells.length){const [r,c]=cells[Math.floor(Math.random()*cells.length)];board[r][c]=Math.random()<.9?2:4}};
const merge=line=>{const a=line.filter(Boolean),out=[];let gained=0;for(let i=0;i<a.length;i++){if(a[i]===a[i+1]){const v=a[i]*2;out.push(v);gained+=v;i++}else out.push(a[i])}while(out.length<SIZE)out.push(0);return[out,gained]};
const rotate=b=>b[0].map((_,i)=>b.map(r=>r[i]).reverse());
const move=dir=>{let b=clone(board),t={left:0,up:1,right:2,down:3}[dir];for(let i=0;i<t;i++)b=rotate(b);let gain=0,n=b.map(row=>{const[m,g]=merge(row);gain+=g;return m});for(let i=0;i<(4-t)%4;i++)n=rotate(n);if(JSON.stringify(n)===JSON.stringify(board))return false;board=n;score+=gain;addTile();check();render();save();return true};
const canMove=()=>{for(let r=0;r<SIZE;r++)for(let c=0;c<SIZE;c++){if(!board[r][c])return true;if(c<3&&board[r][c]===board[r][c+1])return true;if(r<3&&board[r][c]===board[r+1][c])return true}return false};
const check=()=>{if(!won&&board.some(r=>r.includes(TARGET))){won=true;show('2048 reached!','You made it. Keep going for a higher score.')}};
const show=(title,sub)=>{const m=$('#r2048Message');if(!m)return;m.hidden=false;m.querySelector('strong').textContent=title;m.querySelector('span').textContent=sub};
const hide=()=>{const m=$('#r2048Message');if(m)m.hidden=true};
const render=()=>{const g=$('#r2048Grid');if(!g)return;g.innerHTML='';for(let r=0;r<SIZE;r++)for(let c=0;c<SIZE;c++){const v=board[r][c],el=document.createElement('div');el.className='r2048-cell';if(v){el.dataset.value=v;el.innerHTML=`<span>${v}</span>`;el.animate([{transform:'scale(.82)',opacity:.45},{transform:'scale(1)',opacity:1}],{duration:140,easing:'cubic-bezier(.2,.8,.2,1)'})}g.appendChild(el)}$('#r2048Score').textContent=score;$('#r2048Best').textContent=Math.max(best,score);$('#r2048Moves').textContent=Math.floor((Date.now()-startedAt)/1000);if(!canMove()&&!won)show('Run complete','No moves left. Start a new run.')};
const save=async()=>{try{best=Math.max(best,score);const r=await fetch('/api/account/data',{credentials:'same-origin',cache:'no-store',headers:{Accept:'application/json','Cache-Control':'no-store'}});if(!r.ok)return;const j=await r.json(),settings={...(j.settings||{}),game:{...(j.settings?.game||{}),game2048:{bestScore:best,state:{board,score,won,keepPlaying}}}};await fetch('/api/account/data',{method:'PUT',credentials:'same-origin',cache:'no-store',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({settings})})}catch{}};
const load=async()=>{try{const r=await fetch('/api/account/data',{credentials:'same-origin',cache:'no-store',headers:{Accept:'application/json','Cache-Control':'no-store'}});if(!r.ok)return false;const g=(await r.json()).settings?.game?.game2048;if(g){best=Number(g.bestScore)||0;const s=g.state;if(s?.board&&Array.isArray(s.board)&&s.board.length===4){board=s.board.map(r=>r.slice());score=Number(s.score)||0;won=!!s.won;keepPlaying=!!s.keepPlaying;return true}}}catch{}return false};
const newGame=async()=>{board=empty();score=0;won=false;keepPlaying=false;startedAt=Date.now();hide();addTile();addTile();render();await save()};
const finish=async()=>{if(window.RedlighteGame?.finish)await window.RedlighteGame.finish('2048',score,won)};
const start=async()=>{const mount=$('#gameMount'),modal=$('#gameModal');if(!mount||!modal)return;modal.hidden=false;document.body.classList.add('game-open');document.documentElement.classList.add('game-open');mount.innerHTML=`<div class="r2048-wrap"><div class="r2048-top"><div><div class="r2048-kicker">REDLIGHTE ORIGINAL</div><h1>2048 <span>∞</span></h1><p>Merge. Multiply. Break your record.</p></div><div class="r2048-scores"><div><small>SCORE</small><b id="r2048Score">0</b></div><div><small>BEST</small><b id="r2048Best">0</b></div></div></div><div class="r2048-tools"><button id="r2048New">New run</button><span>Swipe or use <b>← ↑ ↓ →</b></span><span>TIME <b id="r2048Moves">0</b></span></div><div class="r2048-board"><div class="r2048-grid" id="r2048Grid"></div><div class="r2048-message" id="r2048Message" hidden><strong></strong><span></span><div><button id="r2048Continue">Keep going</button><button id="r2048Retry">New run</button></div></div></div><div class="r2048-footer"><span>Reach 2048 to win</span><button id="r2048Done">Save & exit</button></div></div>`;$('#r2048New').onclick=newGame;$('#r2048Retry').onclick=newGame;$('#r2048Continue').onclick=()=>{keepPlaying=true;hide()};$('#r2048Done').onclick=async()=>{await save();await finish();window.RedlighteGameClose?.()};let sx=0,sy=0;mount.ontouchstart=e=>{const t=e.changedTouches[0];sx=t.clientX;sy=t.clientY};mount.ontouchmove=e=>e.preventDefault();mount.ontouchend=e=>{const t=e.changedTouches[0],dx=t.clientX-sx,dy=t.clientY-sy;if(Math.max(Math.abs(dx),Math.abs(dy))<25)return;move(Math.abs(dx)>Math.abs(dy)?(dx>0?'right':'left'):(dy>0?'down':'up'))};const restored=await load();if(!restored){board=empty();score=0;won=false;startedAt=Date.now();addTile();addTile()}else startedAt=Date.now();render();document.onkeydown=e=>{if(!$('#r2048Grid'))return;const m={ArrowLeft:'left',ArrowRight:'right',ArrowUp:'up',ArrowDown:'down'}[e.key];if(m){e.preventDefault();move(m)}}};
window.Redlighte2048={start,newGame};})();